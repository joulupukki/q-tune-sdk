# ESP-IDF build artifacts for the example plugin projects
**/build/
**/managed_components/
**/sdkconfig
**/sdkconfig.old
**/dependencies.lock

# Editor / language-server caches
**/.cache/
.vscode/

# OS / tooling junk
.DS_Store
**/__pycache__/
*.pyc

# User plugin projects — the standard place to scaffold your own plugins.
# Kept out of the SDK repo so your work stays separate and survives `git pull`.
/plugins/
